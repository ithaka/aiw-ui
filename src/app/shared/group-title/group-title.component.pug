.row.group-border
    .col-9.pb-24.pt-24.z-1000
        .row.m-sm-0.pb-3(#titlerow="")
            h1.title {{ ig.name }}
        .row.m-sm-0.item-last-updated
            span {{ ig.total }} items
            span &nbsp;&nbsp;•&nbsp;&nbsp;
            span Last updated {{ ig.update_date | date:'MMMM dd, yyyy' }}
    .col-3.pb-24.pt-24.z-1000.center-align
        //- button.btn.btn-secondary.ml-3 Export
        .dropdown.d-inline-block(ngbDropdown, aria-haspopup="true", (openChange)="dropDownOpen = $event")
            button#exportDropdown.btn.btn-secondary((mouseover)="dropDownBtnHovered = true", (mouseout)="dropDownBtnHovered = false",(focus)="dropDownBtnFocused = true", (focusout)="dropDownBtnFocused = false", ngbDropdownToggle, tabindex="2", aria-label="Export group")
              i.icon([ngClass]="[ dropDownBtnHovered || dropDownBtnFocused || dropDownOpen ? 'icon-export-btn-hover' : 'icon-export-btn']")
              | Export
              = " "
            .dropdown-menu(ngbDropdownMenu)
              a#exportPptLink.dropdown-item(tabindex="2", aria-label="Powerpoint", role="button")
                i.icon.icon-powerpoint
                .content
                  label Powerpoint
                  .caption Exports as one image per slide, with metadata in the speaker notes
                
              a#exportGsLink.dropdown-item(tabindex="2", aria-label="Google Slides", role="button")
                i.icon.icon-google-slides
                .content
                  label Google Slides
                  .caption Exports as one image per slide, with metadata in the speaker notes
              a#exportZipLink.dropdown-item(tabindex="2", aria-label="Zip File", role="button")
                i.icon.icon-zip
                .content
                  label ZIP File
                  .caption Items and their metadata are combined as a compressed file

        button.btn.btn-primary.ml-3 Present
